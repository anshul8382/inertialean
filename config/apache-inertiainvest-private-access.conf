# DEPRECATED — do not use for staff access.
#
# Allowing 100.64.0.0/10 on the public vhost does NOT work when staff browse
# https://inertiainvest.in (traffic uses their home/office ISP IP, not Tailscale).
#
# Use instead:
#   config/apache-inertiainvest-public-lockdown.conf  (deny public login)
#   scripts/deployment/setup_tailscale_serve.sh       (staff *.ts.net URL)
#
# This file is kept only for reference.
